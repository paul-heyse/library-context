# ast-grep rules land here from design-review findings (one rule per recurring
# code shape, each with fixtures in rule-tests/). Starts empty on purpose.
ruleDirs:
  - rules
testConfigs:
  - testDir: rule-tests
