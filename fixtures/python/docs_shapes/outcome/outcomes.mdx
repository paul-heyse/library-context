---
title: "Outcomes"
---

## Serving

The `pkg.make_server` factory builds a configured server
from its name. It is the usual start.

## Tools

Everything above is done for you. Then `pkg.Server.tool` registers a tool.
