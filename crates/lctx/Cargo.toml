[package]
name = "lctx"
description = "The production entry point: acquire a library, then extract, derive, validate and publish its snapshot (DESIGN §4.0, ADR-0013)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
cpg-schema = { path = "../cpg-schema" }
cpg-extract = { path = "../cpg-extract" }
cpg-flow = { path = "../cpg-flow" }
cpg-core = { path = "../cpg-core" }
lctx-analytics = { path = "../lctx-analytics" }
lctx-embed = { path = "../lctx-embed" }
clap.workspace = true
anyhow.workspace = true
fs-err.workspace = true
serde_json.workspace = true
getrandom.workspace = true
tokio.workspace = true

[target.'cfg(any(target_os = "linux", target_os = "macos"))'.dependencies]
tikv-jemallocator.workspace = true

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
