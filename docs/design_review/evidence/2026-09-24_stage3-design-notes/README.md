# Stage 3 design notes, 2026-09-24

A working draft on resolving `call_transfer` by summaries (call results as intermediate values),
the models catalog, exceptions, ContextVars and compatibility. The forward plan's Stage 3
supersedes it where they differ; kept for the reasoning.
