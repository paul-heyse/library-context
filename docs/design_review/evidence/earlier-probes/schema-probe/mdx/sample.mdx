---
title: T
---
# Tools

<ParamField body="enabled" type="bool" default="True">
  <Warning>Deprecated in v3.0.0. Use `mcp.enable()` instead.</Warning>
  A boolean to enable or disable the tool.
</ParamField>

<ParamField body="icons" type="list[Icon] | None">
  <VersionBadge version="2.13.0" />

  Optional list of icons.
</ParamField>

<Warning title="Heads up">
`pkg.Server.tool` does not validate `fn`.
</Warning>

```mdx
<Warning>not a component</Warning>
```

Inline `<Warning>x</Warning>` code and text <Tip>tip</Tip> here.
