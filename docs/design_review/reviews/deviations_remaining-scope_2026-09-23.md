# Deviations and judgment calls: the remaining-scope plan

The operator approved the remaining-scope plan (`~/.claude/plans/we-have-not-yet-pure-swing.md`,
2026-09-23) with one instruction: "run straight through all the way through the end without
stopping. If there is a necessary deviation, I trust your judgment call in order to get to
completion, just make sure to note them so that at the end you can share them for my review".

This log is that note. It is append-only, one entry per judgment call or deviation, each with
what was planned, what was done, why, and how to reverse it. It is evidence for the operator's
review, not authority: the decisions it records live in ADRs and DESIGN.

| # | Date | Slice | Planned (design or plan) | Done | Why | Reverse by |
|---|---|---|---|---|---|---|
| D1 | 2026-09-23 | Phase 0 | §12 / plan 5.1: the operator writes or approves the held-out evaluation tasks | A fresh subagent restricted to FastMCP's fetched docs (no briefs, gold, analytics config or design) writes about 20 tasks, fixtures with reference solutions, and expected calls into `eval/heldout/`, sealed by `MANIFEST.sha256` and committed before any brief exists. **Outcome:** 20 tasks (16 in the six subsystem areas, 4 other), 8 offline fixtures with reference solutions (all 8 pass by direct invocation; pytest is not in the library environment), 81 doc quotes verified verbatim; 5 doc inconsistencies noted in its README. One independence caveat: the harness loaded the FastMCP repository's own `CLAUDE.md` into the author's context when it first read a docs file; the author reports using nothing from it | Straight-through execution; sealing before any brief exists keeps the set held out from what it evaluates | Replace `eval/heldout/` with operator-written tasks before increment 5 runs |
| D2 | 2026-09-23 | Phase 0 | DESIGN §1.2: hybrid retrieval (BM25, RRF, promotion, degraded mode) in increment 4 | Moved into increment 1 (slice 1.8), ADR-0004 amendment; increment 4 is the generation lifecycle and serving reliability | The §9.8 keep rule (increment 3) must judge techniques on the retrieval stack that is served; the spike already had it | Revert the ADR-0004 amendment and move the retrieval code behind a mode flag until increment 4 |
| D3 | 2026-09-23 | 1.4 | DESIGN §1.4 / plan 1.4: the operator pre-registers the analytics config (subsystem, public roots, seed, distractors) | A first draft was mine; the ADR-0019 standard review (F3) found that I had read the gold families earlier in the session, so the subsystem and distractors were **rewritten by a docs-only subagent** (FastMCP's docs and the installed package only; no gold, skill, brief or held-out material), applying rules stated before it looked: the server-components surface (16 module prefixes, each justified in the file's comments) and "the seed class's other public methods a server author calls to add a component or behaviour, in the order `servers/server.mdx` introduces them" (`FastMCP.resource`, `FastMCP.prompt`, `FastMCP.custom_route`). The seed `fastmcp.FastMCP.tool` is DESIGN's. The file was committed before any brief existed; its commit is the pre-registration, and the frozen digest is recorded before the first gold scoring (3.3) | Straight-through execution; independence from the gold (G4) | Edit the file (a new config digest, so new run ids); after the first gold scoring, only by ADR |
| D4 | 2026-09-23 | 1.4 | §10.4: from increment 3, a brief is published only after one manual review pass | `[briefs] serve_unreviewed = true`: bundles will serve briefs whose `review_state` is `unreviewed`, with the state shown | The operator's review cannot happen inside a straight-through run; hiding every brief would leave nothing to evaluate | Set it to `false` once `reviews.toml` holds verdicts |
| D5 | 2026-09-23 | 1.4 review | Slice 1.4 compact review F1 left the operator a choice: (a) a nested definition as a stated-model boundary, or (b) a typed "defines" arc Pass A may follow | (b): the invocation projection gains `definition` arcs (function → the function it declares), which Pass A follows like calls and never counts as a `direct_delegation`; `arc_kind` enters the witness step key | (a) would only mark the gap; (b) puts a decorator factory's registered closure, which is where 2 of the 4 pilot seeds do their work, into the neighbourhood that Stage F describes. The Coordinates template says "defines `T`, a nested callable it returns or registers" rather than claiming a call | Drop the third UNION branch in `projection.rs` and the definition templates; the `arc_kind` code stays (codebooks are append-only) |
| D6 | 2026-09-23 | 1.4 review | Review F2: narrow §9.1's seed-resolution sentence to the code, or implement the fail-closed MRO walk | The fail-closed walk: an unresolved ancestor, a non-`def` binding of the name in a class of the chain, or a non-release class before the definition refuses the compile | The narrowing would leave a silent misresolution possible on a second library; the walk costs one query and refuses no pilot seed (checked on the pilot) | Revert `member()` in `analyze.rs` to the first `def` in the MRO and narrow §9.1 |
| D7 | 2026-09-23 | 1.5 review | Slice 1.5 compact review F2 left the operator a choice: what Outcome leg 2 selects, or disable it | Leg 2 takes the lead sentence of the paragraph holding an exact mention, only when the mention lies inside that sentence. Sentences are segmented on a soft-break-joined view; change-log documents (`CHANGELOG_STEMS`) are excluded; a class's export no longer counts as a mention of its method | Disabling would leave every docstring-less seed `unresolved` (151 of 669 public FastMCP methods). The stricter rule is decidable and tested on a corpus fixture, and writing that test exposed the class-export bug, which disabling would have hidden | Make leg 2 return nothing in `synth.rs` (the `else if` branch) and amend DESIGN §10.3 |
| D8 | 2026-09-23 | 1.5 review | Review F5: either DESIGN §10.2's `parameter` row (structural only) or the policy (structural or documented) changes | The policy keeps `documented`; DESIGN §10.2 says so | Increment 2.1 adds parameter docs, which are documentation. The status derivation (F1) now makes `documented` need documented evidence, so the wider policy cannot be abused | Drop `Documented` from the `Parameter` row of `ASSERTION_POLICY` until 2.1 |
| D9 | 2026-09-23 | 1.5 review | Review F4: define "analysis-backed" as a derivation-family finding (delegation, restriction, handoff) | `ANALYSIS_BACKED` = direct and bounded delegation **and** implementation boundaries | An implementation boundary is Pass A's positive observation that the seed calls into named code; excluding it would label a seed whose only calls are into dependencies as documentation alone | Remove `ImplementationBoundary` from `findings::ANALYSIS_BACKED` |
| D10 | 2026-09-23 | 1.5 review | Review F1(3): cite the depth or budget stop, "either as a finding Pass A emits or as `invocation` evidence (a codebook append)" | A finding: `finding_kind` appends `traversal_stop`, which Pass A emits with its stop reason and depth | A finding keeps the stop in the analysis tables with its invocation link, and the no-bypass rule can exclude it by kind; `invocation` evidence would need resolved text for a JSON record | The code is append-only; stop emitting it in `pass_a.rs` and cite the invocation instead |
| D11 | 2026-09-23 | 1.7 | Plan 1.7: the generation key is the first 16 hex of the SHA-256 "over the sorted file digests" | Over the whole manifest without its key: file digests, snapshot, content and compiler digests, spec hash, summary | Two snapshots with identical files but different provenance (another compiler digest) would otherwise share a key while their manifests differ, and the second build would collide with the first | Hash only the `files` entries in `key_of` (`cpg-core/src/bundle.rs`) |
| D12 | 2026-09-23 | 1.7 | Plan 1.7: the bundle reads the snapshot, including `embedding_cache`, and writes `embedding_spec` | The store gained what that needs: `brief_documents.spec_hash` and a per-snapshot `embedding_specs` table (ADR-0019 amended; a declared migration) | The spec was never stored, and one text has a vector per spec (the fake and the vLLM spec share `input_hash`es), so the vectors of a snapshot could not be selected from the store alone | Drop the table and column; the bundle would then need the spec passed in, breaking §6.4's one derivation path |
| D13 | 2026-09-23 | 1.8 | Plan 1.8: the root uv workspace has members `python/lctx_mcp` **and `eval`** | One member, `python/lctx_mcp`. The gold extract is `scripts/gold_extract.py` (as planned) writing `eval/gold/`; `eval/` stays data until its first code | ADR-0001: no package without a consumer. The eval member's first code is the 3.3 scoring | Add `eval/pyproject.toml` and list it in `[tool.uv.workspace] members` |
| D14 | 2026-09-23 | 1.9 | §11.1: the brief document is "outcome, applicable case, public APIs, controls, usage description and limits"; slice 1.5 put every Limits assertion in it, the analysis's boundary lists included | The document carries only Limits-section kinds other than `analysis_boundary` (none in increment 1): what the analysis did not follow stays in the brief, out of retrieval | Slice 1.5 review O7's trigger fired: the §1.5 check put the seed below distractors. The prescribed measurement (live Qwen vectors, 2026-09-23): without the boundary text the seed's vector rank for "Register functions and expose component metadata" went 2 → 1, the other alias unchanged. The change is a category (analysis scope is not a capability limit), not word tuning against the gold; the hybrid rank moved 4 → 2 and §1.5 still fails on that alias (its lexical leg ranks by length on one shared word) | Put `AnalysisBoundary` back in `synth.rs`'s document limits; `TEMPLATE_VERSION` 5 |
| D15 | 2026-09-23 | 1.8/1.9 | Roadmap: a compact review of 1.8, then a deep review after 1.9 | Slice 1.8's compact review was folded into the increment-1 deep review, whose scope lists 1.8 explicitly | 1.8 and 1.9 landed back to back; one fresh reviewer over both saves a pass without losing coverage | Run a compact review of 1.8 alone |
| D16 | 2026-09-23 | 2.0 | Plan 2.0: new seeds are pre-registered by the operator after a seed audit | A mechanical audit, SQL over the published pilot with its rules written in `analytics.toml`, chose `fastmcp.FastMCP.mount` (restriction and handoff). It was committed before any increment-2 brief existed. The client handoff (FastMCP → Client, 531 uses) is outside the pre-registered subsystem, so it is not seeded | Straight-through mode. The rules make the choice reproducible; I have read the gold, so I avoided judgment-based seeding (the D3 lesson) | Edit `[seeds]` in `libraries/fastmcp/analytics.toml` before 2.1's first pilot |
| D17 | 2026-09-23 | 2.1 | Plan 2.1: Pass B with its own parameters, and a 2.0 design-reviewer pass over Pass B/C's stated abstractions before implementing | Pass B reuses `[pass_a] max_depth` (recorded in its invocation's parameters), so `analytics.toml` is unchanged after its pre-registration. The abstraction review is folded into the 2.1 and 2.2 compact reviews, which review the abstractions with their code | A config edit after the seeds were pre-registered would reopen that commitment. A review of abstractions without code would repeat the compact review's work | Add `[pass_b] max_depth` to the config; run a standalone review |
| D18 | 2026-09-23 | 2.1 | §9.2: a `conditional_raise` becomes a public precondition with supporting evidence; `transformed_argument` covers "a literal, default or expression supplied downstream" | Restrictions are always implementation statements (`structurally_observed`), and nothing promotes one. `transformed_argument` covers only literals the seed itself supplies | No precondition-documentation recognizer exists yet (3.4 adds documented warnings, which can carry the promotion). Expressions and deeper literals have no reader that could state them without guessing | Add a promotion rule citing a documented warning (3.4); widen the literal scan in `pass_b.rs` |
| D19 | 2026-09-23 | inc1 deep review | U1 was left to the operator: how fusion treats a leg that cannot discriminate, and the pre-registered retrieval evaluation (an ADR-0010 amendment) | Decided and amended: a leg votes only where it discriminates. The lexical leg scores only words some briefs lack, and abstains without one. The 3.3 evaluation is hit@1 and hit@5 per gold family over the full brief set, with live vectors. §1.5 is restated for 15–25 briefs, and `just ranking-check` exits 1 on a miss | The rationale is independent of the gold: a word in every document carries no information. A constructed test holds it. Waiting for the operator would have left increment 3 without a pre-registered procedure | Revert the `discriminating` filter in `lctx_mcp.retrieval`; amend ADR-0010 |
| D20 | 2026-09-23 | inc1 deep review | U2 was left to the operator: which brief sections are slots, so that absence is counted | Outcome, Public access, Applicable case, Controls, Usage pattern and Limits (`SLOT_SECTIONS`). The manifest counts `absent_slots`, and `get_capability` names `sections_absent` | These are the §10.3 sections every brief is meant to fill. Evidence is implied by the others, and Related is optional. The served "no gaps" was misleading agents now, not later | Edit `SLOT_SECTIONS`; remove the two served fields |
| D21 | 2026-09-23 | inc1 deep review | U3 was left to the operator: where the gold freeze falls, and whether D16's seed edit needs an ADR | The freeze moves to 1.9, the first use of gold against compiled output. ADR-0004's amendment records D16's seed (its author had seen the gold, disclosed) and F3's key removal. The config is re-frozen by digest in `eval/gold/analytics-freeze.json`, which `just gold` checks | The review's recommendation. Keeping the mount seed, which gold-free rules chose, with the overlap disclosed, is more honest than a silent revert | Revert `afef05a`'s seed edit by an ADR-0004 amendment, and update the freeze digest |
| D22 | 2026-09-23 | inc1 deep review F8 | The review cadence: compact reviews of slices 1.6 and 1.7; ADR-0019 accepted by its reviewer or the operator | The 1.6 and 1.7 compact reviews were not run. The increment-1 deep review covered both slices' code (embeddings, bundle), so no further review is owed. ADR-0019 is accepted with evidence `Tested`, as that review recommended | The deep review read both slices end to end, including the bundle's byte identity and the MERGE path. ADR-0019 was built on by six slices, each tested | Run standalone compact reviews of 1.6 and 1.7; set ADR-0019 back to `proposed` (it has no later amendment that depends on acceptance) |
| D23 | 2026-09-23 | inc1 deep review F3 | D4: `[briefs] serve_unreviewed = true`, reversible by setting it to `false` | Removed. It was inert: nothing read it, so D4's reversal did nothing. Bundles serve every brief with its `review_state` until the 3.5 review workflow adds the gate and its consumer. `budget` now refuses more seeds than it allows | A control that does nothing misleads the operator who flips it | Re-add the key with a bundle-side consumer (3.5) |
| D24 | 2026-09-23 | 2.2 | ADR-0019 and §6.4: a `usage_patterns` table joins the analysis group, and a `usage_patterns` file is served | A usage pattern is a `usage_pattern` assertion (section Usage pattern) whose text is the code, citing each statement's verbatim `example` span and the handoff it shows; it is served in `assertions` and `evidence` | Every property a pattern needs is already an assertion's: kind, section, status derived from its supports, verbatim evidence, content identity, grounding rules. A separate table would duplicate them | Add a `usage_patterns` table and bundle file, and move the snippet there |
| D25 | 2026-09-23 | 2.2 | §9.3: "rejected as a handoff: … additional consumers" | A read of the bound value as a method receiver (`x.method()`, `@x.tool`) is setup of the produced object, not another consumer. Any other second read (an argument, a return, a store) still rejects it | Under the literal rule, 146 of FastMCP's 156 `FastMCP(...)` → `mount(server=…)` occurrences failed only because the sub-server gets its tools first (`@sub.tool`). §10.5's pattern is "the operation plus the setup it needs" | Count receiver reads in `reads` (`handoffs_sql`) |
| D26 | 2026-09-23 | 2.1 review | Slice 2.1 compact review U1 (F4) left the author a choice: (a) record each declined flow as a finding with a reason, or (b) amend §4.2.4 to "Pass B declines; nothing records it" | (a). `parameter_reads_sql` finds every argument reading a name the caller binds under a parameter's name. Pass B reports those it does not follow as `unfollowed_argument` (reason `rebound`, `computed` or `unmapped`), and Stage F states them per parameter as an `unfollowed_control` Limits line, kept out of the brief document like `analysis_boundary` (D14). §4.2.4 says the trace is an analysis finding, not a `boundaries` row. A guard declined under a handler stays unstated (§9.2's stated model) | On the pilot, `FastMCP.tool`'s `meta` is copied and then forwarded; under (b) the brief's silence reads as "not forwarded". The §B11 gap metric needs what the analysis did not follow | Stop emitting it in `pass_b.rs` and drop the template; amend §4.2.4 and §9.2 (the codes stay: append-only) |
| D27 | 2026-09-23 | 2.1 review | Review F2 offered to extend a truncated parameter description or to drop and count it. F1(3) offered to decline or to qualify a raise behind a branch | F2: extend. The entry runs to its end by indentation, and its text is then those lines, trimmed. A description no header, or more than one, locates is a `signatures` boundary (`provider_disagreement`), so its module's `signatures` coverage is `partial`. F1: decline a raise when a construct around a call on the path also reads the flowing value, and qualify every other conditional call ("only on some paths"). A `with` counts as a handler, which is conservative | Dropping would lose descriptions that are only cut short (46 on the pilot). A boundary is the store's existing way to say "seen, not captured", and it is counted. Declining every conditional call would lose restrictions that are true when reached; qualifying the contradicted ones would publish raises the seed cannot trigger | Return `None` for `Extended` in `locate_description`; drop `value_tested` from the decline in `pass_b.rs` |
| D28 | 2026-09-23 | 2.3 | ADR-0011 and §9.4: the γ grid, seed count, hub percentile and layer weights are pre-registered in the analytics config | They are code constants (`lctx_analytics::communities::Params::preregistered`). The grid is 0.5, 1, 2, 4 (RBER), with seeds 0–9, hub percentile 0.95, invocation and co-use weights 0.5 each, least agreement 0.5 and at least 2 public members. The choice: the non-degenerate γ with the highest mean pairwise ARI, ties going to the γ nearest 1. The commit before any community output is the pre-registration; every consensus invocation records them in its parameters, and the compiler digest includes them | `analytics.toml` is frozen (D21) and changes only by an ADR-0004 amendment. Code constants keep the config's digest and still pre-register the parameters: they are fixed before any output and recorded in every result | Move `Params` into `[communities]` in `analytics.toml` by an ADR-0004 amendment, and re-freeze its digest |
| D29 | 2026-09-23 | 2.4 | §9.5: the damping, tolerance, iteration budget and dangling target are recorded in the analytics-config digest; the usage projection is declared in §5 | The parameters are pre-registered code (`lctx_analytics::ranking::Params`: 0.85, 1e-10 L1, 100, uniform), recorded in the invocation and the compiler digest, as D28 did for communities. The usage projection is the invocation projection restricted in Rust under a named weight policy, digested with it, rather than a second SQL projection | The config is frozen (D21). The invocation projection already holds the usage callers and their arcs, so a second query would duplicate it | Move the parameters into `analytics.toml` by an ADR-0004 amendment; declare a `usage` `ProjectionSpec` |
| D30 | 2026-09-23 | 2.2 review | The slice 2.2 compact review offered choices: F1 refuse a candidate under a header, or carry the header; F1(d) implement or narrow §10.4's "refers only to existing public APIs"; F3 extend a producer's pattern to its consumer, or cite only what it shows; F4(b) narrow the receiver exclusion, or restate D25 | F1: refuse, plus a free-name check over the assembled code (annotations included; builtins from `ruff_python_stdlib` at the analyzed minor version). F1(d): narrowed; a pattern is verbatim official code. F3: cite only a handoff whose occurrence lies whole inside the pattern, and prefer, within a role, a pattern that shows one. F4(b): the code narrowed to D25's text (a called attribute or a decorator) | Carrying headers (`with pytest.raises`) would publish a test's expectation of failure as usage. Checking each import against the release would re-derive what the snippet's own import already states. Preferring a pattern that shows its handoff whole gives the producer seed a pattern with its consumer, without extending statements forward | Drop the block check and `free_names` in `usage.rs`; cite on `ProducerSite` again in `synth.rs`; widen `reads` in `handoffs_sql` back to every attribute value |
| D31 | 2026-09-23 | ADR-0011 review | The ADR-0011 standard review offered: F2 fix γ = 1, or a margin rule over the grid; F3 keep or drop definition and candidate arcs; F4 freeze the parameters; accept the whole record or split it | F2: γ = 1 fixed (the review's simpler alternative); the grid is a recorded profile with ARI spread. F3: all three arc kinds kept at 1 per arc, named in `Params` and stated in §9.4; the invocation projection's digest joins the community digest. F4: both parameter sets' digests in the gold freeze (ADR-0004 amendment); D28's "before any output" is corrected to "frozen from the review's fixes". F5's score and threshold on the public co-assignment. The whole record accepted after consolidation; F6 (the MRO walk's looser rules) deferred to a second library | γ = 1 is RBER's own scale and removes a choice that the seed block decided. The review measured the margin rule choosing γ 1 in every block anyway. A single-target candidate arc is the call's own target, so keeping it counts real calls | Restore the grid choice with a margin in `consensus`; filter arc kinds in `Input::build`; drop the freeze keys and the ADR-0004 amendment |
| D32 | 2026-09-23 | 2.5 | §9.6: FCA objects are the public APIs of "the subsystem, one module or one class hierarchy"; the plan named no selection rule for a seed's applicable case | A seed's scope is the public namespace its access path names (an exported class's public methods, declared or inherited, or a module's public callables). The parameters (support 2, budget 20,000) are pre-registered code, frozen with the others. A seed's applicable case is the concept holding it with another API, sharing at least two attributes, that has the most (other API, shared attribute) pairs (the two-attribute floor was added after the first pilot run showed `mount` placed with 11 APIs by one shared parameter type; no gold was consulted); its implications are the three best-supported ones whose premise it meets | The namespace is what the seed's access path already fixes, structural and cheap. The whole subsystem would mix unrelated classes and multiply concepts. The pair count prefers a family (`tool`, `resource`, `prompt` sharing 14 attributes) over a closer pair sharing 17 | Scope FCA by module or subsystem in `analyze.rs`; change the ranking in `synth.rs`'s applicable-case block |
| D33 | 2026-09-23 | 2.4, 2.5 | The cadence: a compact review at the end of each slice that adds an analytic (2.4 PageRank, 2.5 FCA), then a compact review at increment 2's end | One compact review at increment 2's end covers 2.4, 2.5 and 2.6 together. The ADR-0011 standard review already read PageRank's design, and D29 records its parameters | The three slices share one consumer path (seed selection and Related in 2.6), so reviewing them together judges the path that uses them. Deferred rows fired by 2.5 (C4 O4 re-raises, C2 O4 raised types) are dispositioned in §9.6 and listed for that review | Run separate compact reviews of 2.4 and 2.5 |
| D34 | 2026-09-23 | 2.6 | §9.4: communities select seeds within the brief budget; the plan did not say which candidates qualify or how selection interleaves with the per-seed passes | Communities and PageRank now run before the per-seed passes. Selection takes, in rounds (communities without a chosen seed first), each community's most central *documented* public API, recorded in a `seed_selection` invocation. Related lists at most five co-members by centrality | An undocumented, call-free API would get a brief with no outcome and no analysis (the no-bypass rule refuses it); the fixture showed two. Rounds spread briefs across communities before deepening one | Drop the docstring filter in `analyze.rs`; select by centrality alone |
| D35 | 2026-09-23 | 3.1 | §9.7 and plan 3.1: kNN with "a fixed `k` and a similarity margin" and a declared, bounded candidate set with a budget in the config | Parameters as pre-registered code, frozen with the others: k 3, a cosine floor of 0.5, and windows of at most 4,096 bytes. The candidate set is the subsystem's public APIs × every corpus passage, recorded per invocation. There is no budget: the exact search over 527,424 pairs is under a second | The config is frozen (D21). A floor rather than a margin: every Qwen cosine sits high, and the ranked top three is what is published. The floor was set before any live run and not tuned after | Move the parameters into the config by an ADR-0004 amendment; add a pair budget to `neighbours::run` |
| D36 | 2026-09-23 | 3.3 | D13: the `eval` workspace member arrives with its first code, the 3.3 scoring | The scorer is `scripts/score_gold.py` over a served generation, reusing `lctx_mcp`'s loader and search, like `ranking_check.py`; `eval/` stays data | One script needs no package (ADR-0001). The agent harness (5.3) is the first code needing its own dependencies | Move the script into an `eval` member when 5.3 creates it |
| D37 | 2026-09-23 | inc2 review | The increment-2 compact review's U1 left the author the centrality consumer's objective, before the budget rises and before 3.3 scores selection; §9.5 and ADR-0011 made delegation PageRank the ranking | Direct usage is the default: each official-usage call site counted once, split evenly among its definite or candidate targets (`USAGE_POLICY`); `direct_usage` findings order selection and Related. PageRank becomes the `+pagerank` variant, off by default. Selection takes eligible APIs (docstring summary, ≥ 1 usage call) by rank with a community cap of ⌈budget / 3⌉. **The first policy, written before any selection ran, counted definite arcs only. A pilot measurement of usage-site modality (11,794 of 11,854 candidate sites have one target; method calls on instances are all `candidate`) showed it would see constructors only, so it was revised, before any selection output, brief or score existed under it.** The first gold scoring (generation 91828e4c) selected nothing (budget = seeds), so no score informed this | The review's recommendation, reasoned from the consumer (which operations users call), not from gold. PageRank over delegation ranks implementation sinks (F3) | Set `pagerank` on by default (`Techniques::default`), and restore the round rule in `selection::select` |
| D38 | 2026-09-23 | inc2 review | U2: (a) the FCA concept as a non-slot line and the Applicable-case slot absent, or (b) keep the slot with a frozen structural floor | (a): a new `shared_signature` kind under Related, from the seed's own scope; the `applicable_case` kind stays, reserved; the slot is absent on every brief until an input-or-mode source exists; "belongs with" becomes "Like … declares …" | A shared signature is not an input or mode (§10.3); counting it as one hid 5 absent slots from the §B11 gap metric, 2 of them coincidental, and put other APIs' names in every chunk's header | Publish `shared_signature` drafts as `applicable_case` again |
| D39 | 2026-09-23 | inc2 review | Fixture edits for the review's tests | `analysis_shapes` gains `pkg.alpha.Alpha(Server)` (F4), `Widgets(Catalog)` with two registration methods (F1), and `Catalog.load`/`reload` with an unresolvable return type raising `KeyError` both ways (F2). `docs_shapes`'s `Server.run` body `return None` becomes the same-length docstring `"""Runs."""`, so the usage-bearing fixture has an eligible unconfigured API without moving any byte offset | The review's probes 3 and 4 and F2/F4's verification rows asked for exactly these shapes; the same-length edit keeps every C5 span snapshot | Revert the fixture lines and the tests that use them |
| D40 | 2026-09-23 | inc2 review | The review's Deferred rows: a `FINDING_METHOD` rule "when any kernel is next edited"; ADR-0019 O6 "one line" | Both done now (ranking was edited): `semantic:finding-kind-by-method` with an injected case, and the in-memory composition stated in ADR-0019's amendments. The valid-JSON `diagnostics` half is not built: every writer is `serde_json::to_string`, so invalid JSON cannot arise | The row's trigger fired | Drop the rule |
| D41 | 2026-09-23 | 3.2 | §9.6: RCA's scaling is "a DataFusion join that adds attribute columns"; §9.4: extra layers "shared parameter types, doc co-mention, kNN", with no weights or policies stated; §9.8: ablation disables one technique | RCA's pairs come from the in-memory projection and Pass C's relation (the same facts, already loaded) rather than a second SQL join. The layers' policies are written in code before any variant ran (`TYPE_LAYER_POLICY`, `MENTION_LAYER_POLICY`, `KNN_LAYER_POLICY`), and with extra layers every layer weighs equally (`EXTRA_WEIGHT_RULE`). A variant can switch several techniques at once (`+a,-b`) and is labelled canonically | The projection already holds every call arc; a second join would duplicate the invocation projection's accepted-evidence filter. Equal weights are the one rule that needs no tuning. Several switches at once let 3.3 compare the whole off-by-default set against the default | Replace `relational`'s inputs with SQL, or weight layers otherwise (an ADR-0004 amendment after the freeze) |
