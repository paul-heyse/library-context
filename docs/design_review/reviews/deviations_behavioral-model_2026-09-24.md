# Deviations and judgment calls: the behavioral-model plan

The operator's instruction (2026-09-24): "Please proceed to implement the entirety of the plan in
docs/plans/behavioral-model-pivot-plan_2026-09-24.md".

This log records the decisions the plan asked the operator to make, and every judgment call made
while implementing it.
- Entries are append-only and numbered B1, B2, … so they cannot be confused with the plan's
  decisions D-1 to D-9 or the review's findings F1 to F14.
- The log is evidence for the operator's review, not authority. The decisions it records live in
  ADRs and in DESIGN.

| # | Date | Stage | Planned (design or plan) | Done | Why | Reverse by |
|---|---|---|---|---|---|---|
| B1 | 2026-09-24 | 0.2 | Plan §4: the operator decides D-1 to D-9 before Stage 0's ADRs | **Every decision takes the plan's recommendation.** D-1: supersede ADR-0004. D-2: a time-boxed `ty_python_core` spike, falling back to our own CFG. D-3: the runtime view. D-4: pyarrow over materialized rows. D-5: closed atoms and five verdicts. D-6: keep a curated subset of briefs. D-7: Stage 3.1's models list. D-8: continue R2 and Phase 3, pause Phase 4's deletion exit and Phase 5 B3/B6. D-9: declared extra families | The operator asked for "the entirety of the plan", and the plan states one recommendation per decision. No option was chosen that the plan did not recommend | Any decision can be revisited by amending the ADR that records it (Stage 0.3) |
| B2 | 2026-09-24 | 0.1 | Stage 0.1: read the `.snap.new`, accept it, run `just test-all`, then commit R2 | The operator had already committed the R2 fixes, the review, the plan and the pivot document together (`8d4c442`, "commit before behavioral plan implementation"). Stage 0.1 reduced to verifying that baseline: `just test-all` **passed** (nextest 217/217, pytest 63, rule tests, deps, gold); `just pilot` **passed** (snapshot `39cc8ce5`, generation `4a6b3d55`, 32.2 s, smoke 20/20) | The snapshot the R2 rule needed was already in the committed tree | — |
| B3 | 2026-09-24 | 0.5 | Stage 0.5: the question set is written from library source and docs before any Stage 1 output is read | A research subagent wrote `eval/behavior/fastmcp-4.0.5.toml`. It was restricted to the installed FastMCP source and the fetched docs, and barred from `.claude/`, the store and the generations. It was given the names of the 20 operations that have briefs, so that the Stage 1 questions ask about operations without one | Stage 1's exit criterion concerns operations with no brief. Knowing *which* operations have briefs selects questions; it does not inform any target's content | Rewrite the Stage 1 questions without the list |
| B4 | 2026-09-24 | 0.5 | Stage 0.5: targets written from library source and docs | The question-set subagent also **ran** a probe script against the installed FastMCP 4.0.5 to confirm nine claims: a sync resource template runs on the event loop; a tool timeout is masked; `tasks=True` is ignored by `add_tool` and by `tools=[...]`; the lifespan reruns for a sequential client; the middleware order; `run_async(transport="stdio", host=...)` raises `TypeError`; the SSE app has no Host/Origin guard; a parent's masking does not cover a mounted child; `uvicorn.Config` refuses a duplicate `host`. The set is 20 questions and 134 items, every citation checked | Executing the library is evidence about the library, not compiler output, so pre-registration holds. It caught four places where the docs disagree with the 4.0.5 source; those became negative items | — |
| B5 | 2026-09-24 | 0.5 | Review §5 journey b and Appendix B Q4: `mounted_components_raise_on_load_error` is never read | **Corrected** by an erratum appended to the review: `fastmcp_tasks/lifespan.py:58` reads it. The journey had searched only the `fastmcp` package. Q04.f carries the correct fact | Found while verifying the pre-registered targets | — |
| B6 | 2026-09-24 | 1.1–1.3 | Plan Stage 1.1–1.3: a `behavior` **fact family** written by a Stage D derivation, with a `coverage` row per public callable | The nine tables are **analysis tables** (the findings group, ADR-0019), written after `public_paths` and before Stage E. Coverage is `operations.behavior_status` (a verdict), held by `semantic:behavior-covers-public` | The relations read the analytics config's public roots and Pass B's depth budget, so they are compiler outputs, like `public_paths`, not extracted facts. `coverage` rows are per extractor run and module | Declare a family and move the writes to Stage D |
| B7 | 2026-09-24 | 1.2 | Plan: the `verdict` codebook and `semantic:refuted-needs-complete-region` land in Stage 2 | Both land in Stage 1. The rule's Stage 1 premise is the operation's own scan (`behavior_status` established); Stage 2 adds the region's boundaries | Stage 1 rows already need verdicts (unfollowed reads are `unknown`, override-open delegations are `unknown`). The ADR review's F2 flagged the gap | — |
| B8 | 2026-09-24 | 1.7 | Plan / ADR-0010 amendment: the view id and version go inside the input's identity | The view is a **column** (`operation_documents.embedding_view`), and a cache key stays the request text's hash. Two views with the same text share a vector, which is correct | One text has one vector per spec. Folding the view into the key would embed identical texts twice. The ADR review's F10 accepted this wording | Add the view to the key |
| B9 | 2026-09-24 | 1.4 | Plan: facets from existing facts | Parameters, declared types, returns, raises and decorators come from `concepts::attributes_sql`, FCA's declared attributes, as their one authority. `raises` means a typed `raise` directly in the body. Behavioral facets come from `behaviors`. The all-techniques guard moved as a declared migration (nine tables and one invocation); the analysis ledger did not | One relation per meaning (DM-02). A second attribute SQL would drift from FCA's | — |
| B10 | 2026-09-24 | 1.9 | Plan Stage 1.9: build the packet, assess it, and have the operator review it | After reading the first packet, `get_operation` on a class was extended to carry its constructor's record (`__init__`). Classes answered nothing otherwise, and Pass C's handoffs attach to `__init__`. The assessment rates the extended packet. **The targets were not touched**: the set is pre-registered in `2fb10b8` and unchanged. The operator's review of `structured_eval_stage1_2026-09-24.md` is **outstanding** (it cannot happen inside a straight-through run) | Pre-registration fixes the targets, not the system. Improving the system after reading outputs is how the stages are meant to proceed | Drop `constructor` from `Operation` |
| B11 | 2026-09-24 | 2.1 | Plan Stage 2.1: the D-2 spike is time-boxed, in a worktree, with exit tests for range parity, `flow_shapes` known answers, reaching definitions within our candidates, and the added time and memory | **ty chosen.** The spike ran in a scratch crate, outside the repository, over FastMCP 4.0.5's 275 release modules: 213 ms and 47 MiB peak. **Parity:** 36 of our 39,986 references had no ty use, and all 36 are augmented-assignment targets the probe's visitor skipped (`x += …`). 13,077 ty uses are annotation names, which we do not record. **Reaching definitions:** 27,782 within our candidates (2,319 strictly narrower) and 251 outside, all from two focus conventions: import aliases (`a as b`), and ty's synthetic loop-header definitions. 11,917 have no local definition. **ty decides `TYPE_CHECKING` as true** at index time (`resolve_to_literal`), so the runtime view needs a same-length token rename before ty parses. The `flow_shapes` known answers become `cpg-flow`'s tests instead of spike output | The spike answered the plan's exit questions. The worktree bought nothing, because the spike crate had no path into the workspace. The fixture's answers belong in the crate that must keep them | Revert to our own CFG builder (ADR-0022's option 3 allows either) |
| B12 | 2026-09-24 | 1 (revise) | Increment 3's deep review, F2: either define `behavior_status` over the scan's region (own override-open calls optional) or narrow and rename it | **The region definition, with own override-open calls counted**, and unfollowed reads counted as a boundary. On the pilot `established` falls from 991 to 570 operations | ADR-0022 names this status as the premise for forward-chain refutations; a narrower status would have to be renamed and a second one built for Stage 2. Counting own override-open calls is what "met no boundary" says | Drop own override-open calls from the status (`behavior.rs`, the delegation loop's `meet`) |
| B13 | 2026-09-24 | 1 (revise) | Increment 3's deep review, F7: put the embedded views under the keep criterion or pre-register `search_operations` items | **Pre-registered items** Q23 and Q24 (the views stay); if neither earns a present rating at Stage 2's evaluation, the source-body view is removed. A live embedding measurement waits for a free GPU | The pilot uses fake vectors, so a keep-rule ablation now would measure nothing | Remove `EmbeddingView::SourceBody` rows from `operation_documents` |
| B14 | 2026-09-24 | 2.1 | Deleting nothing outside the session's own outputs | The disk was full (3.4 GB free on a 1.6 TB volume shared with other work). `target/debug/incremental` (8.7 GB, cargo's incremental cache) was removed; the store was moved aside to `build/store-pre-revise-2026-09-24` with its embedding cache copied, as at Stage 1 | The pilot and builds need room; the cache regenerates. The two moved-aside stores (`build/store-pre-stage1-2026-09-24`, 1.5 GB; `build/store-pre-revise-2026-09-24`) are kept for the operator to delete | — |
| B15 | 2026-09-24 | 2 (evaluation) | Plan Stage 2 exit and the pre-registered stage-2 rule over Q04, Q10, Q21–Q24 | **Attempt 1 failed** (generation `004c6c93…`): Q10.f was misleading, because `Client(name=…)` was served as established in a helper's result that only logs it. The cause and five more defects were fixed, and ADR-0022 (still proposed) was amended in place: (1) a use inside a call is `through_call`, and its claims are `unknown` with `call_transfer` (boundary reason 19, appended); (2) nested fallbacks keep their conditions; (3) self-subsuming resolution in the normalizer, the product of a function's guards factored out, ambient reads on the normal path; (4) versioned places (`place@line`) where a scope tests a rebound place at two values; (5) an opaque test names only its roots; (6) an unmapped argument names its release callee. **Attempt 2 passed** (generation `85304572…`): 12 present, 6 partial, 2 absent, nothing incorrect or misleading, no negative claimed. `structured_eval_stage2_2026-09-24.md` | The rule prescribes pass or fail per attempt; ADR-0021's revisit needs two consecutive failures. Four of the six defects were found by reading Stage 1's questions on the fixed generation, not by a Stage 2 item | Revert the Stage 2 evaluation commit; the codebook entry stays (append-only) |
| B16 | 2026-09-24 | 2.6 | Plan Stage 2: every fate carries its condition | **Fates are stated on the normal path.** A guard that raises (`if x is None: raise`) is factored out of the conditions of the fates past it (`Condition::given`), and the guard is its own `raises_when` fate. At the evaluation (B15), factoring extended to the product of guards and to ambient reads | Without it, every fate past a guard carries the guard's negation, which says only that no error was raised; Q04's and Q21's conditions were unreadable | Drop `normal_path` (`flow_model.rs`) |
| B17 | 2026-09-24 | 2.6 | DESIGN §3.2: a stored field's reads in any relative's method compose at depth 2 | **Only a value stored unchanged composes** with the field's later reads | A computed store holds a different value; composing its reads would attribute them to the parameter | Remove the `unchanged` filter in `behavior.rs` |
| B18 | 2026-09-24 | 2.6 | Plan Stage 2's behavior kinds: derives, stores, returns, reads_setting, is_read | **`tests` added** (`behavior_kind` 12, appended): a parameter that decides a branch lists the literals its function's tests compare it with | A parameter that only gates a delegation (`show_banner`) had no fate, although Q14.g and Q10 ask exactly that | Stop emitting it (the code stays, append-only) |
| B19 | 2026-09-24 | 2.6 | ADR-0022 §Places: a root resolves through reference resolutions | **A module's own global binding wins over a submodule of the same name** (`fastmcp.settings` is the `Settings()` instance, not `fastmcp/settings.py`) | The package rebinds the name at import, which is what the runtime sees; all 46 of the pilot's settings reads depended on it | `resolve` in `flow_model.rs` |
| B20 | 2026-09-24 | 2.6 | Stage 1 (B10): a class record carries its constructor's | **A class record lists no parameters of its own**; its constructor's record (`__init__`), shown inside it, carries them with their fates | The class record listed every constructor parameter again with no fates, beside the constructor's record that has them | `_record` in `operations.py` |
| B21 | 2026-09-24 | 3 (preparation) | Plan Stage 3.4: Pysa, which takes its types from Pyrefly, as a differential oracle | **The operator's decision: ty stays the flow provider and Pysa stays the cross-check** (Pysa as the L3 provider was weighed and set aside). Verified in a scratch spike: Pysa now lives at facebook/Pysa, still ships in `pyre-check` (0.10.0, 2026-08-06) and uses Pyrefly by default; on FastMCP 4.0.5 it ran offline in 8.5 s at 702 MiB, 2,051 models. It picks up whatever `pyrefly` is on `PATH`, so the oracle must pin `--pyrefly-binary` | Pysa exposes no reaching definitions or branch conditions (L1's needs); as L3 it would lose conditions and assume flow through unknown callees | Revisit only with an ADR |
| B22 | 2026-09-24 | 2 (end review) | The Stage 2 end review's corrections (its §8 alternative) | Judgment calls in carrying them out: (1) "may catch" is decided by the builtin exception tree plus the release's MRO, and any unresolvable handler or raised name may catch (sound, less precise); (2) `with suppress(...)` suppresses and other context managers are assumed not to, until Stage 3's models; (3) the test authority is a new flow table, `flow_tests`, fed from ty's predicates, so `tests` now also covers conditional-expression and boolean-operand tests; (4) an abstract, stub or raise-only body gets a new boundary reason, `abstract_body` (20), rather than reusing `override_dispatch`; (5) a `__dict__` load's `dynamic_accesses` site is its flow use's id; (6) the `unreachable-not-established` injected case rewrites `true` to `false` in `conditions`, because `analysis_shapes` has no `TYPE_CHECKING` declaration and adding one would move the analysis ledger | Each follows the review's recommended direction with the least new surface | ADR-0022 §Conditions, §Places, §Verdicts |
| B23 | 2026-09-24 | 2.9 (plan) | The pivot plan's Stages 3–5, and its condition language (closed atoms, DNF, "no solver") | **The operator approved the forward plan's decisions D-10 to D-13**, as recommended in the external review's assessment: (D-10) condition atoms are evaluations, with the Python operator preserved and sharing only for provably stable values; (D-11) a decision-diagram condition kernel (biodivine-lib-bdd 0.6.3, after Stage 3.0's spike), amending §B10's wording so that theory solvers stay excluded; (D-12) a validation lane that executes only generated programs, isolated, never the analyzed library or `fixtures/python/`; (D-13) a typed theory from Pyrefly's types, for builtin immutable scalars only. The pivot plan is superseded by `docs/plans/behavioral-model-forward-plan_2026-09-24.md` | The assessment reproduced eight translation defects that drop feasible paths, confirmed by CPython 3.14.7 (`design_review_external-review-assessment_2026-09-24.md`) | ADR-0022 (D-10, amended in Stage 2.9); a new ADR at Stage 3.0 (D-11) |
| B24 | 2026-09-24 | 2.9 compact review | D-10 proposed sharing identity tests on names with equal reaching-definition sets | **Every source test is per evaluation site.** Definition sets remain flow facts, and the parser reserves their encoding for a future proved-stable rule. The Stage 3 typed theory will not equate tests across sites from immutable-scalar types alone | The review's executed `nonlocal` counterexample changed `x` through a nested call while ty reported the same binding; the translator had dropped a feasible return as `false`. Rebinding an `isinstance` class and the five-field `sys.version_info` exposed two more unsound cases. Site identity admits all three, at a possible precision cost to be measured in the pilot | Add an effect-stability proof and a separate ADR before allowing cross-site sharing |
