# Stage 3 first modeled return summary — compact review

## 1. Scope, purpose and coverage

| | |
|---|---|
| Subject | Direct synchronous identity-model return producer, canonical proof steps and summary-boundary complement |
| Standard | Core 2.0, code-intelligence 1.0, library-context binding |
| Tier · purpose | Change · conformance |
| Reviewer · date | Codex · 2026-09-25 |
| Decision | Accept the narrow modeled may-flow; retain other paths as unknown |

I inspected source/model/argument joins, BDD implication, ordered proof-id recipe, writer,
shared validator, boundary SQL, and positive/withholding fixture. Assignment predecessors,
nested calls, callback/resource/exception fates, SCC composition, FORMAT 7 and the real-library
pilot were not examined by this review.

## 6. Gates

| Gate | Verdict | Evidence or scope reason | Required action |
|---|---|---|---|
| G1 Authority | Pass | Pysa/source target, Ruff/lexical call syntax, typed model assertion, ty value/exit facts and the BDD catalog retain separate authorities. | — |
| G2 Fidelity | Pass, scoped | A sole closed definite target, normal-return assertion, exact whole-expression identity, ordered arguments and direct return are each required. Dynamic/shadowed siblings and nested expressions do not pass. | Continue with assignment and L2 fate proofs separately. |
| G3 Validity | Pass | Shared validation reconstructs finite rows and ordered steps; rule references close cited inputs. | Add targeted tamper for each future step kind. |
| G4 Hidden behaviour | Pass, scoped | DataFusion selects source/model facts; the existing bounded kernel decides implication; Rust only assembles the finite proof. One hydrated catalog is shared by direct and modeled producers. | Watch hydration cost on the pilot. |
| G5 Consistency | Pass | Output version 61 and append-only proof-step codebook mark the migration. | — |
| G6 Transformation | Pass | Canonical id hashes typed ordered evidence; local call facts and model rule remain in the step relation. | Preserve this identity in interprocedural composition. |
| G7 Claims | Pass, scoped | Verdict is a may-flow under the stated source/model abstraction; no negative follows from absent proof. Remaining returns keep a `summary_boundaries` row. | Do not infer total execution or effect coverage. |
| G8 Library leverage | Pass | DataFusion relational selection and `Diagram::implies` avoid duplicate condition or Python execution engines. | — |
| CI-G1 Fidelity | Pass, scoped | The positive exact call and unknown dynamic/shadowed/raising-sibling/nested cases diverge in focused fixture. | Challenge callee rebinding with runtime oracle. |
| CI-G2 Evidence closure | Not applicable | FORMAT 7 does not yet serve these rows. | Trace proof steps into native serving. |
| CI-G3 Evaluation integrity | Pass | Local fixture and pinned model inputs do not read the FastMCP gold reference. | — |

## 7. Findings and principle verdicts

| ID | Finding | Principles · gate | Evidence and consequence | Correction |
|---|---|---|---|---|
| F01, deferred | Lexical callee resolution and Pysa's closed target set are claims within the declared source model, not a proof against arbitrary runtime rebinding. | DP-08, CI-06 · CI-G1 | A mutated global can take an unmodeled runtime exit; the admitted path is a may-flow, not total behavior. | Keep model-relative wording and challenge runtime cases in the independent oracle. |
| F02, deferred | The current producer accepts only direct body returns. | DP-09, CI-07 · CI-G1 | Assignment, nested frames and cross-call composition remain unknown; treating their silence as absence would be unsound. | Add their own finite proof producers and preserve explicit boundaries. |

DP-01/02/03/04/05/08/09/11/13/14/15/18/19/21/22/23/24 and CI-01/02/04/06/07/10/12 are satisfied for this narrow source-model may-flow. Graph, heuristic and serving principles are outside this slice. No SHOULD exception is requested.

## 8. Library-leverage ledger

| Capability | Bespoke scope | Built-in feature | Fit |
|---|---|---|---|
| Source/model selection | Domain proof preconditions | DataFusion joins and generated relations | No second call-target inference. |
| Path condition admission | Model-relative condition choice | Existing bounded BDD `implies` | No DNF rendering or string comparison. |
| Proof identity | Typed ordered domain steps | Existing `lctx_id` recipe | Same endpoints with different evidence stay distinct. |

## 12. Decision

**Accept the first positive modeled return producer within the stated model.** Focused release
Nextest (`registry_snapshot`, `rules_snapshot`, `derivations_snapshot`,
`pinned_identity_models_require_and_publish_their_real_formals`) passed 4/4 on 2026-09-25 after
snapshot review. The fixture asserts one exact modeled flow and no flow for dynamic, shadowed,
raising-sibling, nested or Boolean-fallback cases; it also runs publication tamper checks. Targeted Clippy and
the schema reference checks complete this slice. `just fmt`, `just test-all`, `just pilot` and
integrated evaluation remain `not_run` by operator direction until all functional Stage 3 scope
is implemented.
